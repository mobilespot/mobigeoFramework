import XCTest
@testable import mobigeoFramework

final class mobigeoFrameworkTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(mobigeoFramework().text, "Hello, World!")
    }
}
